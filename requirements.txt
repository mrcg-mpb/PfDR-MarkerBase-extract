# Pipeline dependencies (used by process.yml / pipeline.py).
# The weekly digest (digest.py) needs none of these — it's stdlib only.
google-api-python-client
google-auth
anthropic
pydantic
pyyaml

# Supplement-file conversion (src/supplements.py): spreadsheets and Word docs.
# Legacy .doc additionally uses LibreOffice (soffice), preinstalled on the runner.
openpyxl        # .xlsx
xlrd            # .xls (legacy Excel)
python-docx     # .docx
